<h1>Editing post</h1>
<div class = "container">

<%= render 'form' %>

<p>	<%= link_to 'Show', @post, class: 'btn btn-mini' %>
		<%= link_to 'Back', posts_path, class: 'btn btn-mini' %>
		<%= link_to 'Delete',@post, method: :delete, data: { confirm: 'Are you sure?' }, class: 'btn btn-mini' %>
</p>

</div>